namespace ExtremeEditor.App;

public sealed partial class MainForm
{
    // Timing diagnostics remain available in ExtremeEditor.Core for regression
    // tests and targeted investigations, but are intentionally not exposed in
    // the normal editor UI.
    private void InitializeTimingProbeUi(ToolStrip tools)
    {
    }
}
